# Why back.obyw.one/_/ Is Blocked

This document explains WHY the admin URL is blocked. <!-- pb-admin-bypass: ok -->

The back.obyw.one/_/ path is blocked via Caddy because it must only be accessed <!-- pb-admin-bypass: ok -->
over an SSH tunnel using `shi bridge open back`.
